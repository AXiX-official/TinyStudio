<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:TinyStudio.ViewModels"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="TinyStudio.Views.AssetListView"
             x:DataType="vm:MainWindowViewModel">
    <DockPanel>
        <Grid DockPanel.Dock="Top" Margin="5" ColumnDefinitions="*,Auto">
            <TextBox Watermark="Search..." Grid.Column="0" Margin="0,0,5,0"/>
            <Button Content="Search" Grid.Column="1" HorizontalAlignment="Left"/>
        </Grid>
        <DataGrid ItemsSource="{Binding LoadedAssets}"
                  SelectedItem="{Binding SelectedAsset}"
                  Margin="5"
                  RowHeight="14"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  CanUserResizeColumns="True">
            <DataGrid.Styles>
                <Style Selector="DataGridCell">
                    <Setter Property="Padding" Value="2,2"/>
                </Style>
            </DataGrid.Styles>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name"
                                    Binding="{Binding Name}"
                                    FontSize="12"
                                    Width="200"
                                    MinWidth="100"/>
                <DataGridTextColumn Header="Type"
                                    Binding="{Binding Type}"
                                    FontSize="12"
                                    Width="120"
                                    MinWidth="60"/>
                <DataGridTextColumn Header="PathID"
                                    Binding="{Binding PathId}"
                                    FontSize="12"
                                    Width="120"
                                    MinWidth="50"/>
                <DataGridTextColumn Header="Size"
                                    Binding="{Binding Size}"
                                    FontSize="12"
                                    Width="100"
                                    MinWidth="50"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
